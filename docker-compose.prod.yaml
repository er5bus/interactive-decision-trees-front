version: '3'

services:
  decision_trees_prod:
    build:
      context: .
      dockerfile: Dockerfile.build
    volumes:
      - ./public:/app/public
      - ./src:/app/src
    env_file:
      - ./.env
    ports:
      - "80:80"

