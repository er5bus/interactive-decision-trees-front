version: '3'

services:
  decision_trees_dev:
    build:
      context: .
    volumes:
      - ./public:/app/public
      - ./src:/app/src
    env_file:
      - ./.env
    ports:
      - "3000:3000"
    tty: true
